# Agents Configuration

## Code Style Rules

### Comments
- **ALL comments MUST use hash style (`#`), NOT docstrings (`"""` or `'''`)**
- Use `#` for all inline comments, function documentation, and block comments
- Do not use triple-quoted docstrings anywhere in the codebase
